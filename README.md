Dear reviewers,

The scripts are in the scripts/ folder. Each subfolder is a distinct analysis. Scripts are ordered from 1-N for each step, which should be run in sequence. You can also start any script and it'll run. 

Raw data is in the data/ folder.

Processed, analysis-ready data is in the builds/ folder.
